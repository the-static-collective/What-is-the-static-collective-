# Lawful Navigation Specimen 001 — Design

## Purpose

Publish one human-readable Front Room evidence specimen for the first landed executable cross-repository NAV crossing:

```text
Project0 → Corpus OS
```

The specimen is a projection of executable evidence already landed in Project0. It does not define a new navigation protocol, grant traversal authority, or replace the executable fixture.

## Governing compression

> **Evidenced nearby door → deliberate crossing → bounded changed-field witness.**

## Source authority

The document must be derived from the landed Project0 lineage, not from memory or this repository's issue text alone.

Primary executable source:

- Project0 landed commit: `1c4decea33e0d22babb4a48be29cf5034c9318f9`
- fixture: `fixtures/nav/lawful-navigation-project0-corpus-os.ts`
- acceptance test: `tests/nav-lawful-navigation-specimen.test.ts`

Pinned evidence carried by that fixture:

- Front Room threshold law: `patterns/field-traversal-and-illumination#threshold-probing-is-not-traversal`
- Authority Kit commit: `b99dd1bf3e9af4c30a4f0e365237357086b7fdf6`
- Authority Kit registry relation: `corpus-os CONFORMS_TO project0`
- Project0 source frame commit: `0961e44f7fabc5807acea2b267009230f1e846c3`
- Corpus OS destination frame commit: `f54c808c3c91a599f47189a1e873c8adcaff7143`
- Corpus OS Latent Reachability blob: `e31f97d27a16c15a79ea3062dfdad2214413cc81`

## Truth boundary

The specimen must preserve the exact distinctions demonstrated by the landed test:

1. `Project0 → Corpus OS` is `materially_changed`.
2. The field/frame changes.
3. Constitution remains `indeterminate` because both `constitutionRef` values are `null`.
4. Authority remains `preserved` with empty before/after authority refs.
5. Decoder and participant remain preserved.
6. Evidence changes because Corpus OS Latent Reachability becomes visible only after crossing.
7. `particularity:current-project` changes.
8. `particularity:nearby-door` remains preserved.
9. `particularity:prospective-reachability` is `new_after`.
10. The experimental crossing receipt remains `nav-...`, never canonical `rect-...` identity.

The prose must not strengthen any of those claims.

## Placement

Create one new page under the existing **Evidence** section:

`specimens/lawful-navigation-specimen-001.md`

Add it to `SUMMARY.md` adjacent to the Threshold Scan specimen.

Add one bounded follow-up link from `patterns/field-traversal-and-illumination.md` immediately after the threshold-probing laws or in the smallest nearby location that clearly marks this as a specimen rather than a new Pattern.

## Page shape

The evidence page should contain:

- purpose and source pins;
- why Corpus OS was an evidenced nearby door;
- the deliberate crossing declaration;
- a compact before / crossing / after witness table;
- what materially changed;
- what explicitly did not change;
- the newly visible Latent Reachability particularity;
- receipt/identity boundary;
- residual fog and non-goals;
- a final bounded verdict.

Prefer direct prose and a compact table over new GitBook custom blocks. No new visual language is required.

## Authority law

`CONFORMS_TO` explains why Corpus OS was a relevant neighboring destination. It does not grant permission.

Latent Reachability is destination evidence discovered after crossing. It does not authorize NAV, execution, or future consequence.

Repository commits identify bounded frames/evidence. They are not promoted into governing constitutions.

## GitBook / GitHub boundary

This repository is the Git-synced source for GitBook space `HILTtUulCBDqDzXXk6RQ`. The current Git Sync configuration is active against `the-static-collective/What-is-the-static-collective-` on `main`.

Therefore the implementation should land through GitHub and let Git Sync publish the page. Do not create a second competing GitBook change-request version of the same content.

## Non-goals

- no executable NAV code in the Front Room repository;
- no shared navigation package;
- no auto-traversal or recursive scout;
- no route ranking or optimization;
- no `TraversalWarrant`;
- no universal world/multiverse graph;
- no claim that `CONFORMS_TO` grants authority;
- no claim that Latent Reachability grants navigation or execution authority;
- no claim that repository commits are constitutions;
- no replacement of the Project0 executable fixture with prose.

## Validation

Before PR completion:

1. re-read the landed Project0 fixture and test at commit `1c4decea33e0d22babb4a48be29cf5034c9318f9`;
2. confirm the new page and both links use real repository paths;
3. confirm `SUMMARY.md` references the page exactly once;
4. scan for authority/constitution overclaims and accidental universalization;
5. verify the diff is docs-only and limited to the design/plan, one specimen page, `SUMMARY.md`, and the bounded Pattern link;
6. inspect GitBook Git Sync status without treating pre-merge sync as proof of the branch content being published.
