# Haunted Toaster — alpha.8 Implementation Map

The approved alpha.8 release design is implemented on [GitHub PR #126](https://github.com/the-static-collective/the-haunted-toaster/pull/126). GitHub remains implementation authority; this page is the published orientation layer.

## Test-candidate status

Remote test head: `e83a6512be1b845a553a74dc3f9f0488f4082a65`.

* Version identity: `0.5.0-alpha.8`.
* Full renderer proof: 144-script check, 277/277 unit tests, full production FFmpeg smoke, optional-image/MP3 stream-copy smoke, and a selected six-up winner rendered with an exact accepted receipt.
* Browser witness: eight canonical Chromium states visually inspected at 1380 × 900 and committed as reviewed baselines; CI is comparison-only.
* Vercel: generated production-renderer witness is Ready at the exact test head.
* Windows: distinct Setup and Portable executables built and uploaded at the exact test head in [workflow run 31692261790](https://github.com/the-static-collective/the-haunted-toaster/actions/runs/31692261790); artifact `Haunted-Toaster-Windows-31692261790` is retained for 90 days.
* Packaged field interaction: **PASS** on two human-driven installed-appliance renders from GitHub PR merge commit `1f181ee2cb36491dab5c43e0e01314517f32c172` (head `e83a651…` over base `56e006…`).
* Promotion: PR #126 remains open. It is not merged, tagged, or released. Candidate interaction is proved; exact landed-main identity is not yet proved.

## Dependency line

```
failure evidence floor -----------┐
                                  |
UI Witness Gate                   |
  -> Toastmoods                   |
       -> Native Color v1         |
             -> combined witness -┴-> alpha.8 test candidate
```

Failure evidence is integrated as insurance. UI Witness precedes Toastmoods. Native Color follows Toastmoods because Native Color relationship preference consumes canonical Toast Feel identity.

## Implemented map

1. **Failure evidence floor** — abnormal score-driven FFmpeg exits retain sanitized sibling evidence under schema `full-measure.render-failure.v1`.
2. **UI Witness Gate** — production renderer assets generate the Vercel/Playwright witness under policy `ui-witness-v1`.
3. **Toastmoods / Seven Toast Feels** — six bounded ordinary pressure fields plus **MADD CLOWN CRAZY SLOTS**.
4. **Native Color Witness v1** — canonical image profile, exact `echo` / `counterpoint` relationships, and at most one return window.
5. **Integration / package gate** — exact alpha.8 capabilities, reviewed UI pixels, Vercel witness, Windows package proof, and installed-appliance field receipts.

GitHub carries the executable plans under `docs/superpowers/plans/`, the approved creative design under `docs/superpowers/specs/`, and the future UI update protocol at `docs/UI_CHANGE_PROTOCOL.md`.

## Frozen implementation invariants

* VisualScore v1 is not rewritten merely to carry Toast Feel or Native Color identity.
* Toast Feel domain truth has one CommonJS authority and crosses into renderer UI through main/preload IPC rather than duplicated browser furniture.
* Ordinary Toast Feel pressure rebuilds the current semantic timeline stack so Possession Arc is not lost.
* MADD CLOWN delegates to existing `visible-outcome-stomp-v1`; no second chaos engine.
* Native Color v1 has exactly `echo` and `counterpoint`.
* Native Color is expressive-renderer-only; old/non-expressive timelines remain unchanged.
* Candidate preview and production share the same timeline compiler.
* Universal H.264 remains the required release transport; Efficient HEVC remains optional/experimental.
* The final alpha.8 tag may be created only from the exact main commit proven by browser witness, Windows package, and field specimen.

## Packaged field witness — 2026-08-13

Two installed-appliance runs now cover both sides of the new entry surface: one **MADD CLOWN CRAZY SLOTS** specimen and one ordinary **Ash Bloom** specimen.

### 9. The Tabs We Leave Open

* Toast Feel: `MADD CLOWN CRAZY SLOTS`.
* STOMP lineage: `visible-outcome-stomp-v1` with a recorded seed parent.
* Native Color: `echo`, one bounded decompression window.
* Atmosphere: smoke, 161 compiled events.
* Possession Arc: three transitions across material, camera, and palette.
* Color Drift: six section-relative stops.
* Listener: 95 source lines, 84 timed cues, 10 review flags, 0 human corrections.
* Output: accepted H.264 Witness Window, 528,157,772 bytes, 66 ms source/output duration delta.

### 2. Apricots on Ararat

* Toast Feel: `Ash Bloom` ordinary bounded feel.
* Native Color: `counterpoint`, one bounded decompression window.
* Base atmosphere: firefly, 921 compiled events.
* Lyric Resonance: two bounded production events — `dust` and `smoke`.
* Possession Arc: three transitions across motion, material, and camera.
* Color Drift: six section-relative stops.
* Listener: 54/54 matched, 7 review flags, 9 human corrections.
* Output: accepted H.264 Witness Window, 352,324,276 bytes, 46 ms source/output duration delta.

### Cross-specimen illumination

Both resolved timelines report `patchCount: 0` and `entropySpent: 0` while still expressing Possession Arc, Color Drift, Native Color, primitive-field behavior, atmosphere, and—on Apricots—Lyric Resonance.

That is useful evidence that alpha.8 creativity can live in deterministic, receipt-bearing compiler and policy layers rather than requiring arbitrary ordinary timeline mutation.

The MADD CLOWN specimen is especially important: its receipt preserves STOMP ancestry instead of turning the wild path into an untraceable second chaos engine.

The Apricots specimen is the first packaged field receipt in this sequence showing lyric-triggered atmosphere families surviving into production evidence: the base firefly field admitted bounded dust and smoke visits derived from timed lyrics.

## UI witness disposition

```
UI impact: visual + behavioral + bridge
browser witness: PASS @ e83a6512be1b845a553a74dc3f9f0488f4082a65
visual delta: expected and reviewed
packaged witness required: yes
packaged witness: PASS @ PR merge commit 1f181ee2cb36491dab5c43e0e01314517f32c172 (2 accepted human field renders)
GitBook ontology changed: no
```

## Stop line

The packaged interaction gate is now satisfied for the alpha.8 candidate. The two receipts prove that the new Toast Feel surface reaches accepted render evidence, that Native Color plans survive into the executed timeline/receipt, and that browser-candidate source and packaged-appliance behavior are connected by a recorded build identity.

Release completion still has a distinct identity boundary:

1. land PR #126 only with explicit landing approval;
2. build/prove the exact landed `main` commit rather than treating GitHub's temporary PR merge commit as release authority;
3. only then create the alpha.8 tag/release from that exact proven main identity.

Then ship and return to field mining instead of absorbing alpha.9 research.
