# Boot the House — Composed Proof v0.1

**Status:** project-backed composed machine proof landed on 2026-08-17; human Garden witness remains open. This page records executable evidence, not universal protocol authority.

## What landed

Full Measure now carries the first inhabited Boot the House surface on `main`.

The machine-verifiable path is:

```
Full Measure Garden
  -> bounded visible doors
  -> raw pointer stroke
  -> TranchNode intent decoding
  -> explicit crossing confirmation
  -> Project0 encounter addressing / verification
  -> Corpus OS destination-local disposition
  -> Full Measure residue
  -> changed Garden projection
```

The relevant landed floors are:

* Full Measure Garden implementation: PR #14 -> `cb99bfd7ca240d031ba33da96211e35cdb579f80`
* Full Measure composed proof: PR #16 -> `05cdfd61780eba41cb9ddb314a814013ccb48a7c`
* TranchNode raw-point Intent Stroke stdio v0.2: `bf886c0b4938a1444a79afb7a7b384e91b5d5197`
* Project0 World Encounter stdio: `6341b0223f2b57148d617dcc98d1e0d0c68e14a5`
* Corpus OS destination-local admission: `63c0be4cd49c383ae167ded99103b79ba4626416`

## What the composed proof establishes

The real four-repository CI specimen checks out and installs the participating repositories, boots the actual Full Measure server, and crosses repo-owned local-process boundaries without donor mocks.

The resulting proof distinguishes:

* `admitted`
* `refused`
* `indeterminate`
* destination `failed`
* pre-destination `validation-failed`

Only admitted destination output references become constituted destination state. Refusal, indeterminacy, failure, and validation failure remain inspectably different outcomes.

The proof also preserves these boundaries:

* TranchNode decoding carries `authority: none`;
* gesture ranking does not authorize crossing;
* explicit confirmation remains separate;
* Project0 validation occurs before destination invocation;
* Corpus OS evaluates destination-local disposition without transferring authority to Full Measure;
* reconstruction remains explicitly partial rather than inventing destination-private state or a master graph.

## What is still not proved

The landed composed receipt explicitly records:

```
humanWitness: false
```

Therefore machine proof does **not** satisfy the human acceptance gate.

The remaining specimen is one actual person using the configured Garden UI to:

1. draw a pointer gesture toward Corpus;
2. inspect TranchNode's non-authoritative candidate;
3. explicitly choose **Cross this door**;
4. receive the destination-local disposition;
5. inspect the resulting residue and evidence chain.

Full Measure issue #6 remains the authority for that human gate.

## Consequence for the incubator

The earlier question — whether the bounded primitives could survive one multi-repository heartbeat without authority capture or semantic copying — now has a positive machine specimen.

That does **not** graduate the larger Physics of Meeting horizon into constitutional law. It does move Boot the House from a design-only candidate to project-backed executable evidence.

The next distinct frontier is adjacency discovery: replace the truthfully labeled pinned door fixture with bounded, evidenced nearby-door output from Founder Node / Pollen Scout only after that donor surface is independently landed and compatible.
