# Human Witness Relay — Evidence That Compounds

**Status:** implementation green on GitHub PR #22; founding live human specimens still pending. This page remains a draft witness/projection, not executable authority.

> Human action is scarce. Evidence should compound.

## Current executable state

Human Witness Relay v0 now has a credential-free Node.js implementation in the neutral coordination repository. The relay binds one human observation to exact execution identity, preserves `pass | fail | ambiguous`, assigns deterministic event identity, and renders project-native evidence packets through exact registered adapters.

The relay itself does not close a gate, merge, release, deploy, or make this GitBook page authoritative.

## Founding adapters

The first two declared routes remain deliberately bounded:

* **Haunted Toaster PR #146** — three packaged field specimens: quiet/spacious, dense/mastered/distorted, and Linear positive control.
* **Autodiscography Vault PR #9** — one-real-WAV preservation witness with stable provider/run/receipt/hash/byte/artifact references.

Both adapters refuse stale exact-head evidence and retain project disposition as `pending-project-admission` until the owning project records its own conclusion.

## Fresh verification witness

The implementation was driven through RED → GREEN cycles and most recently verified locally on Node 22.16.0:

```
40 tests / 40 pass / 0 fail
```

The completion review also found and closed a security seam: signed/query/fragment URL values are now rejected before persistence/output, in addition to credential/session-shaped field names, without reflecting sensitive values in validation errors.

## Preserved boundary

At the post-implementation check, the product heads awaiting human proof were still unchanged:

* Toaster #146: `944169c7f7bbd821f51fa8e404302cbaa8f4a342`
* Vault #9: `0232ddf0e59e1b8148538a3c97b3527a76a8d008`

Relay code lives outside those product branches, so preparing the relay did not invalidate the human witness targets.

## What has not happened yet

No real human observation has been routed through the relay yet. Therefore this page must not claim that either product gate has passed or that the relay has a live founding specimen.

The next real Toaster or Vault human action may become that first specimen. GitHub project-owned evidence must exist before any later GitBook projection says the corresponding gate changed state.

## Authority

Executable implementation/design authority: GitHub PR #22 and the owning product repositories.

This GitBook change request is a downstream witness only and should remain draft until the implementation is landed and/or a project-owned live specimen gives it something new to project.
