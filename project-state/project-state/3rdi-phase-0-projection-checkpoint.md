# 3rdi — Phase-0 Projection Checkpoint

**Projection status:** project-backed executable Phase-0 checkpoint. GitHub/3rdi remains canonical.

The `the-static-collective/3rdi` Phase-0 hatch landed on 2026-08-27 as merge commit `191cf9986adb24b54400a2d14fe103142edf87ea` from exact reviewed head `c856a67bf88da983e24905dd88b61ae50cfd0b9b`.

The landed surface is a deterministic observer-local projection organ. Its working contract is:

> **See from somewhere without pretending it was everywhere.**

It keeps these coordinates and relations distinct:

```
occurrence != availability != attention role != relevance
relevance != causation
carrier != decoder != projection
projection != source != authority
actual future != anticipated future
gate result != side effect
```

The Phase-0 repository proof includes the portable `3rdi` skill, pure JSON projection compiler, temporal cuts, edge exposures, independent causal/relevance cones, decoder/location receipts, five adversarial hatch labs, and repository-owned validation. The exact landing evidence records successful unit/contract tests, deterministic hatch labs, and canonical historical-cut compilation.

This checkpoint does **not** promote 3rdi into a truth service, authorization layer, production event store, telemetry system, UI, numerical CRS engine, or metaphysical claim. Fresh-model invocation reliability also remains outside the repository proof.

A nearby GitBook frontier, _Knowability Cone — Truth Without Omniscience_, may consume this landed projection organ as evidence of observer-local visibility. That frontier does not acquire 3rdi authority by reference.

**Canonical project landing:** `the-static-collective/3rdi#1`\
**Merge commit:** `191cf9986adb24b54400a2d14fe103142edf87ea`

This page is only a bounded projection checkpoint. If it diverges from project-owned repository evidence, the repository wins.
