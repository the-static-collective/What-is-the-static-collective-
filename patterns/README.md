# Patterns

Reusable laws, architectures, methods, and maxims live here.

A pattern should travel across projects without requiring the reader to inherit one project's implementation. Prefer statements about invariants, relationships, and repeatable methods over framework choices or code details.

When a pattern becomes implementation-specific, its canonical form belongs in the project that implements it.
